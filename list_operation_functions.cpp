#include<bits/stdc++.h>
using namespace std;

int main(){
    list<int> l = {1,2,6,3,4,5,5,5};

    // l.remove(5);
    // l.sort();
    // l.sort(greater<int>());
    // l.unique();
    // l.reverse();

    for(int val : l){
        cout << val << " ";
    }
    return 0;
}